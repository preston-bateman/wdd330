portarray = [
    {
        label: "Week 1 Notes",
        url: "week1/index.html"
    }
];
for (let i=0; i < portarrar.length; i++) {
    let x = document.createElement("li");
    x.innerText = "";
    document.getElementById("table-content").appendChild(x);
}
